# Environment variables for Docker Compose development

# SMTP (for sending verification emails)
SMTP_HOST=smtp.yandex.ru
SMTP_PORT=465
SMTP_USER=viktor.rox@yandex.ru
SMTP_PASS=XqGmE23nWTu44Rf
SMTP_FROM=viktor.rox@yandex.ru
PORT=8080
NODE_ENV=development

# DynamoDB Local (for development)
DOCUMENT_API_ENDPOINT=http://dynamodb-local:8000
DOCUMENT_API_REGION=ru-central1
AWS_ACCESS_KEY_ID=local
AWS_SECRET_ACCESS_KEY=local
USE_DATABASE=true
